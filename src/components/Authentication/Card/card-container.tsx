const CardCotaniner = ({ children }: { children: React.ReactNode }) => {
  return (
    <main className="flex items-center justify-center pt-14">{children}</main>
  );
};

export default CardCotaniner;
